# MongoDB connection string (your cluster + database name)
MONGODB_URI=mongodb+srv://Jeevashre_G:jeeva1165@cluster0.kiwid.mongodb.net/recipe?retryWrites=true&w=majority



# JWT secret used for authentication tokens
JWT_SECRET=secret_text


# Environment mode
NODE_ENV=development

# SMTP (email) credentials for sending mails (Brevo in your case)
SMTP_USER=93d128001@smtp-brevo.com
SMTP_PASS=ncJ0S9m31qKk2rbV
SENDER_EMAIL=jeevashre1210@gmail.com

# Backend server port
PORT=4000

# Frontend origin for CORS
CLIENT_ORIGIN=http://localhost:5173

# Optional admin credentials (used for auto-creation if configured in code)
ADMIN_EMAIL=admin@recipedia.com
ADMIN_PASSWORD=admin123
